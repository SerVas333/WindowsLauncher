<!-- ===== WindowsLauncher.UI/Styles/ModernComponents.xaml - СОВРЕМЕННЫЕ UI КОМПОНЕНТЫ ===== -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========== СОВРЕМЕННЫЕ КАРТОЧКИ И КОНТЕЙНЕРЫ ========== -->
    
    <!-- Базовая карточка с тенью -->
    <Style x:Key="CorporateCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CorporateSurface}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Effect" Value="{StaticResource CorporateShadowDepth1}"/>
    </Style>
    
    <!-- Приподнятая карточка -->
    <Style x:Key="CorporateElevatedCard" TargetType="Border" BasedOn="{StaticResource CorporateCard}">
        <Setter Property="Effect" Value="{StaticResource CorporateShadowDepth2}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Effect" Value="{StaticResource CorporateShadowDepth3}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Интерактивная карточка -->
    <Style x:Key="CorporateInteractiveCard" TargetType="Border" BasedOn="{StaticResource CorporateCard}">
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Effect" Value="{StaticResource CorporateShadowDepth2}"/>
                <Setter Property="Background" Value="{StaticResource CorporateGray50}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ========== CHIP И BADGE КОМПОНЕНТЫ ========== -->
    
    <!-- Базовый Chip -->
    <Style x:Key="CorporateChip" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CorporateGray100}"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="4,2"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    
    <!-- Активный Chip -->
    <Style x:Key="CorporateChipActive" TargetType="Border" BasedOn="{StaticResource CorporateChip}">
        <Setter Property="Background" Value="{StaticResource CorporateRed}"/>
    </Style>
    
    <!-- Badge для счетчиков -->
    <Style x:Key="CorporateBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CorporateError}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="MinWidth" Value="20"/>
        <Setter Property="MinHeight" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    
    <!-- Badge текст -->
    <Style x:Key="CorporateBadgeText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="9"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <!-- ========== СОВРЕМЕННЫЕ ПОЛЯ ВВОДА ========== -->
    
    <!-- Поле ввода с плавающей меткой -->
    <Style x:Key="CorporateFloatingLabelTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource CorporateSurface}"/>
        <Setter Property="Foreground" Value="{StaticResource CorporateOnSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CorporateGray300}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="0,16,0,8"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="MinHeight" Value="56"/>
        <Setter Property="VerticalContentAlignment" Value="Bottom"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource CorporateRed}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== НАВИГАЦИОННЫЕ КОМПОНЕНТЫ ========== -->
    
    <!-- Кнопки табов -->
    <Style x:Key="CorporateTabButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource CorporateTextMuted}"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource CorporateText}"/>
                            <Setter Property="Background" Value="{StaticResource CorporateGray50}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource CorporateGray100}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Активная кнопка таба -->
    <Style x:Key="CorporateTabButtonActive" TargetType="Button" BasedOn="{StaticResource CorporateTabButton}">
        <Setter Property="Foreground" Value="{StaticResource CorporateRed}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CorporateRed}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- ========== ДИАЛОГИ И ВСПЛЫВАЮЩИЕ ОКНА ========== -->
    
    <!-- Фон диалога -->
    <Style x:Key="CorporateDialogOverlay" TargetType="Border">
        <Setter Property="Background" Value="#80000000"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    
    <!-- Контейнер диалога -->
    <Style x:Key="CorporateDialogContainer" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CorporateSurface}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="Margin" Value="32"/>
        <Setter Property="MinWidth" Value="320"/>
        <Setter Property="MaxWidth" Value="560"/>
        <Setter Property="Effect" Value="{StaticResource CorporateShadowDepth4}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ========== ПРОГРЕСС ИНДИКАТОРЫ ========== -->
    
    <!-- Круглый прогресс индикатор -->
    <Style x:Key="CorporateProgressRing" TargetType="ProgressBar">
        <Setter Property="IsIndeterminate" Value="True"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Foreground" Value="{StaticResource CorporateRed}"/>
        <Setter Property="Background" Value="{StaticResource CorporateGray200}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Ellipse x:Name="circle"
                             Stroke="{TemplateBinding Background}"
                             StrokeThickness="3"
                             Width="{TemplateBinding Width}"
                             Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Линейный прогресс -->
    <Style x:Key="CorporateProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="4"/>
        <Setter Property="Foreground" Value="{StaticResource CorporateRed}"/>
        <Setter Property="Background" Value="{StaticResource CorporateGray200}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="2"
                            Height="{TemplateBinding Height}">
                        <Border x:Name="PART_Track"
                                Background="Transparent"
                                CornerRadius="2">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"/>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== СПИСКИ И ЭЛЕМЕНТЫ ========== -->
    
    <!-- Элемент списка -->
    <Style x:Key="CorporateListItem" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource CorporateGray200}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CorporateGray50}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Заголовок группы списка -->
    <Style x:Key="CorporateListGroupHeader" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource CorporateTextMuted}"/>
        <Setter Property="Margin" Value="16,16,16,8"/>
    </Style>

    <!-- ========== РАЗДЕЛИТЕЛИ ========== -->
    
    <!-- Горизонтальный разделитель -->
    <Style x:Key="CorporateHorizontalDivider" TargetType="Rectangle">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Fill" Value="{StaticResource CorporateGray200}"/>
        <Setter Property="Margin" Value="0,8"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>
    
    <!-- Вертикальный разделитель -->
    <Style x:Key="CorporateVerticalDivider" TargetType="Rectangle">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Fill" Value="{StaticResource CorporateGray200}"/>
        <Setter Property="Margin" Value="8,0"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>

    <!-- ========== TOOLTIPS ========== -->
    
    <!-- Современный tooltip -->
    <Style x:Key="CorporateTooltip" TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource CorporateGray800}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>